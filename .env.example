# Copia este archivo a `.env` y ajusta los valores locales.
# Variables con prefijo NG_APP_ se inyectan en build/serve vía `ng --define`.
# No commitees `.env` (está en .gitignore).

NG_APP_API_KEY=your-local-api-key
